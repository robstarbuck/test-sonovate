import styled from "styled-components";

const Content = styled.section`
  max-width: 1020px;
  margin: 0 auto;
`;

const Trunk = styled.main`
  max-width: 1200px;
  margin: 0 auto;
  padding: var(--tile);
`;

export const Layout = {
    Trunk,
    Content
}


